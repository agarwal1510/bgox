#include <stdio.h>
#include <fcntl.h>
#include <unistd.h>
#define COMM_LEN 100
int main(int argc, char *argv[], char *envp[]){

//char buffer[COMM_LEN];
//int line = 0;
//int i = 0;
/*while (read(0, &buffer[i], 1) > 0) {
    if (buffer[i] == '\n') {
        buffer[i] = 0;
        line++;
        i = 0;
		puts(buffer);
        continue;
    }
    i++;
}*/
return 0;
}
